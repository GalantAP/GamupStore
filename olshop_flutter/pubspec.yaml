name: olshop_flutter
description: A new Flutter project.
publish_to: 'none' # Prevent publishing to pub.dev (private package)

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  google_fonts: ^6.1.0
  intl: ^0.18.0
  carousel_slider: ^5.0.0
  lottie: ^2.3.2    # Tambahkan package Lottie untuk animasi json

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/banner/
    - assets/animation/   # Tambahkan ini untuk asset Lottie kamu
